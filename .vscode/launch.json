{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Pipeline (local)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/pipeline/main.py",
            "args": [
                "--bucket=client-model",
                "--project_id=corsino-marketing-labs",
                "--date=2026-04-11"
            ],
            "python": "${workspaceFolder}/.venv/Scripts/python.exe",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
